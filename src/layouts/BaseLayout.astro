---
/**
 * BaseLayout — the shell every page renders through: <html>, global styles,
 * Header, page content, Footer, CookieConsent. Pages pass SEO props straight
 * through to SeoHead (docs/BUILD.md §9).
 */
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import SeoHead from '../components/SeoHead.astro';
import CookieConsent from '../components/CookieConsent.astro';
// Self-hosted fonts (SIL OFL, shipped by @fontsource — no third-party CDN).
// Fraunces: display serif for headings; Inter: body/UI sans.
import '@fontsource/fraunces/400.css';
import '@fontsource/fraunces/400-italic.css';
import '@fontsource/fraunces/500.css';
import '@fontsource/fraunces/600.css';
import '@fontsource/inter/400.css';
import '@fontsource/inter/500.css';
import '../styles/global.css';
// Hashed URLs of the two above-the-fold-critical files, preloaded below so the
// hero headline and first body text never flash a fallback face.
import frauncesPreload from '@fontsource/fraunces/files/fraunces-latin-500-normal.woff2?url';
import interPreload from '@fontsource/inter/files/inter-latin-400-normal.woff2?url';

export interface Props {
  title: string;
  description: string;
  canonicalUrl: string;
  ogImage?: string;
  noindex?: boolean;
  jsonLd?: object | object[];
  transparentHeader?: boolean;
}

const {
  title,
  description,
  canonicalUrl,
  ogImage,
  noindex = false,
  jsonLd,
  transparentHeader = false,
} = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="theme-color" content="#f3eee7" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="preload" href={frauncesPreload} as="font" type="font/woff2" crossorigin />
    <link rel="preload" href={interPreload} as="font" type="font/woff2" crossorigin />
    <SeoHead
      title={title}
      description={description}
      canonicalUrl={canonicalUrl}
      ogImage={ogImage}
      noindex={noindex}
      jsonLd={jsonLd}
    />
  </head>
  <body>
    <a href="#main" class="sr-only focus:not-sr-only focus:absolute focus:left-4 focus:top-4 focus:z-50 focus:bg-ocean focus:px-4 focus:py-2 focus:text-paper">
      Skip to content
    </a>
    <Header transparent={transparentHeader} />
    <main id="main">
      <slot />
    </main>
    <Footer />
    <CookieConsent />
    <script>
      // Reveal-on-scroll for elements marked .fade-in-up (global.css). A tiny
      // vanilla IntersectionObserver — no framework/island needed, keeps
      // content-page JS near zero (docs/BUILD.md §5 performance budget).
      const io = new IntersectionObserver(
        (entries) => {
          for (const entry of entries) {
            if (entry.isIntersecting) {
              entry.target.classList.add('is-visible');
              io.unobserve(entry.target);
            }
          }
        },
        { threshold: 0.15 }
      );
      document.querySelectorAll('.fade-in-up').forEach((el) => io.observe(el));
    </script>
  </body>
</html>
