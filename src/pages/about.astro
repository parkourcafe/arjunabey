---
import { getEntry } from 'astro:content';
import BaseLayout from '../layouts/BaseLayout.astro';
import Hero from '../components/Hero.astro';
import { canonicalFor } from '../lib/seo';

const page = await getEntry('pages', 'about');
const { Content } = await page.render();
const d = page.data;
---

<BaseLayout
  title={d.seo?.title ?? 'About | Anjuna Bay'}
  description={d.seo?.description ?? 'The story of Anjuna Bay.'}
  canonicalUrl={canonicalFor('/about')}
  transparentHeader={true}
>
  {
    d.heroImage && (
      <Hero
        image={d.heroImage}
        imageAlt="The Anjuna Bay estate above Thomas Beach"
        eyebrow="About"
        headline={d.title}
        height="tall"
      />
    )
  }
  <div class="container-content section">
    <div class="prose-anjuna max-w-prose">
      <Content />
    </div>
  </div>
</BaseLayout>
